% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rwilcox_opencl.R
\name{dwilcox_opencl}
\alias{dwilcox_opencl}
\alias{pwilcox_opencl}
\alias{qwilcox_opencl}
\alias{rwilcox_opencl}
\title{The Wilcoxon Rank Sum Distribution (OpenCL)}
\usage{
dwilcox_opencl(
  x,
  m,
  nn,
  log = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

pwilcox_opencl(
  q,
  m,
  nn,
  lower.tail = TRUE,
  log.p = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

qwilcox_opencl(
  p,
  m,
  nn,
  lower.tail = TRUE,
  log.p = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

rwilcox_opencl(n, m, nn, fallback = FALSE, verbose = FALSE)
}
\arguments{
\item{x}{Numeric scalar quantile (must be >= 0).}

\item{m}{Number of observations in one sample (must be > 0).}

\item{nn}{Number of observations in the other sample (must be > 0).}

\item{log}{\code{log} flag for densities (\code{stats} \emph{d}-family semantics).}

\item{opencl_parallel}{Dispatch hint \code{(TRUE,FALSE,NA)} for \emph{p}/\emph{q}
wrappers on this page; parallel kernels reserved.}

\item{fallback}{When \code{TRUE} while \code{\link{has_opencl}()} reports OpenCL present, recover with CPU if the OpenCL call fails. Ignored when the runtime reports no OpenCL (CPU path is chosen automatically). Defaults to \code{FALSE}.}

\item{verbose}{Logical; print fallback/error diagnostics.}

\item{q}{\code{p*}-wrapper quantiles (\code{stats::pwilcox} semantics).}

\item{lower.tail, log.p}{Tail/log-\emph{p} inputs (\code{stats} meanings).}

\item{p}{\code{q*}-wrapper probabilities (\code{stats::qwilcox} semantics).}

\item{n}{Draw-count scalar (\code{r*} path only).}
}
\value{
Numeric vector result from the corresponding Wilcoxon-family operation.
}
\description{
OpenCL-backed density, distribution, quantile, and random generation wrappers
for the Wilcoxon rank sum distribution.
}
\section{Known OpenCL limitations}{

Wilcoxon kernels can still hit runtime-shim gaps depending on device and
driver stack (for example unresolved runtime symbols in some builds).
Defaults follow \verb{fallback = FALSE}: OpenCL failures surface unless you pass \code{fallback = TRUE}.
}

\examples{
if (has_opencl()) {
  # Wilcoxon OpenCL kernels are currently known to fail on some GPU stacks due
  # to unresolved runtime symbols. Keeping these commented avoids flaky checks:
  # n <- 1L
  # dwilcox_opencl(n, x = 5, m = 5, nn = 7, fallback = FALSE, verbose = TRUE)
  # pwilcox_opencl(q = 5, m = 5, nn = 7, fallback = FALSE, verbose = TRUE)
  # qwilcox_opencl(rep(0.8, n), m = 5, nn = 7, fallback = FALSE, verbose = TRUE)
  #
  # Known allocator/runtime fragility on some stacks:
  # rwilcox_opencl(n, m = 5, nn = 7, fallback = FALSE, verbose = TRUE)
} else {
  message("OpenCL unavailable; skipping GPU-only example.")
}
}
