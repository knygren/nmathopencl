% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rexp_opencl.R
\name{rexp_opencl}
\alias{rexp_opencl}
\title{OpenCL-backed exponential random generation with graceful fallback}
\usage{
rexp_opencl(n, rate = 1, fallback = TRUE, verbose = FALSE)
}
\arguments{
\item{n}{Number of observations. Non-negative integer scalar.}

\item{rate}{Rate for the exponential distribution (must be > 0).}

\item{fallback}{Logical; if \code{TRUE}, fall back to \code{\link[stats]{rexp}}
when OpenCL is unavailable or fails.}

\item{verbose}{Logical; print informational fallback messages.}
}
\value{
Numeric vector of length \code{n}.
}
\description{
OpenCL-backed exponential random generation with graceful fallback
}
\examples{
if (has_opencl()) {
  rexp_opencl(5, rate = 1, fallback = FALSE, verbose = TRUE)
} else {
  message("OpenCL unavailable; skipping GPU-only example.")
}
}
