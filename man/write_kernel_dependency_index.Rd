% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opencltools_reexports.R
\name{write_kernel_dependency_index}
\alias{write_kernel_dependency_index}
\title{Build and save a kernel dependency index}
\usage{
write_kernel_dependency_index(
  library_dir = NULL,
  tags = NULL,
  output_path = NULL,
  write = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{library_dir}{Library root with annotated \code{.cl} files.\cr
When \code{tags} is supplied, must agree with \code{tags$library_dir}.}

\item{tags}{Optional attachment result from \link[opencltools]{attach_kernel_dependency_tags}.\cr
Must have \code{ok = TRUE} when reused to skip resorting.}

\item{output_path}{Path for the RDS file. Defaults to
\verb{file.path(<library_dir>, "kernel_dependency_index.rds")}. The \code{.tsv} file is
always written to the same directory with the \code{.tsv} extension.}

\item{write}{If \code{FALSE}, builds the index object and returns it without
writing either file.}

\item{verbose}{If \code{TRUE}, emits short messages with the output paths.}
}
\value{
Invisibly, the index \code{list()} (\code{version} schema):
\itemize{
\item \code{version}: integer schema version.
\item \code{generated_at}: timestamp from \code{\link[=Sys.time]{Sys.time()}}.
\item \code{library_dir}, \code{library_name}: resolved library path / basename.
\item \code{stems_ordered}: stems in global load order.
\item \code{load_order}: named integer vector \code{stem -> rank}.
\item \code{depends}: named list \code{stem -> character()} (direct \verb{@depends}).
\item \code{all_depends}: named list \code{stem -> character()} (transitive dependencies, order
consistent with global load order).
\item \code{n_files}: file count.
}
}
\description{
Writes two companion index files next to the \code{.cl} files in the kernel
library directory:
}
\details{
\itemize{
\item RDS helper shard for loaders such as \link[opencltools]{load_library_for_kernel}.\cr
\item Tab-separated (\code{.tsv}) stem map for C++ (rows \verb{stem<TAB>dependencies}, pre-sorted).\cr
}

Both files encode the same information and are always written together so
they remain in sync.
}
\examples{
############################ Start of write_kernel_dependency_index example ########################

lib_dir <- system.file("cl/nmath_small", package = "opencltools")
idx <- write_kernel_dependency_index(library_dir = lib_dir, write = FALSE)
names(idx)
length(idx$stems_ordered)
idx$n_files

###############################################################################
## End of write_kernel_dependency_index example
###############################################################################
}
\seealso{
\link[opencltools]{load_library_for_kernel}

\link[opencltools]{extract_library_subset}

Other OpenCL kernel library subsets: 
\code{\link[opencltools]{extract_library_subset}()},
\code{\link[opencltools]{load_library_for_kernel}()},
\code{\link[opencltools]{load_library_for_kernel_cross_package}()}
}
