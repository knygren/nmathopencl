% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binomial_opencl.R
\name{dbinom_raw_opencl}
\alias{dbinom_raw_opencl}
\alias{dbinom_opencl}
\alias{pbinom_opencl}
\alias{qbinom_opencl}
\alias{rbinom_opencl}
\title{The Binomial Distribution (OpenCL)}
\usage{
dbinom_raw_opencl(
  x,
  size,
  prob,
  qprob = NULL,
  log = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

dbinom_opencl(
  x,
  size,
  prob,
  log = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

pbinom_opencl(
  q,
  size,
  prob,
  lower.tail = TRUE,
  log.p = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

qbinom_opencl(
  p,
  size,
  prob,
  lower.tail = TRUE,
  log.p = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

rbinom_opencl(n, size, prob, fallback = FALSE, verbose = FALSE)
}
\arguments{
\item{x}{Numeric scalar quantile.}

\item{size}{Number of trials (must be >= 0).}

\item{prob}{Probability of success in \code{[0, 1]}.}

\item{qprob}{Complementary probability. If \code{NULL}, uses \code{1 - prob}.}

\item{log}{\code{log} density switch for \code{dbinom*} wrappers (\code{stats} semantics).}

\item{opencl_parallel}{Dispatch hint \code{(TRUE,FALSE,NA)} for \emph{p}/\emph{q}
wrappers on this page; parallel kernels reserved.}

\item{fallback}{When \code{TRUE} while \code{\link{has_opencl}()} reports OpenCL present, recover with CPU if the OpenCL call fails. Ignored when the runtime reports no OpenCL. Density \code{dbinom_*} wrappers default \code{FALSE}; \code{pbinom_opencl} defaults \code{TRUE} temporarily (\file{inst/OPENCL_PGAMMA_UTILS_KERNEL_FALLBACK.md}); quantile and random wrappers default \code{FALSE}.}

\item{verbose}{Logical; print fallback/error diagnostics.}

\item{q}{Numeric vector of quantiles for \code{pbinom_opencl}; recycled like \code{stats::pbinom}.}

\item{lower.tail, log.p}{Tail/log-\emph{p} inputs (\code{stats} meanings).}

\item{p}{Probabilities for \code{qbinom_opencl}; ordering matches \code{stats::qbinom}.}

\item{n}{Number of observations (non-negative integer scalar). Used only by \code{rbinom_opencl}.}
}
\value{
Numeric vector result from the corresponding binomial-family operation.
}
\description{
OpenCL-backed density, distribution, quantile, and random generation wrappers
for the binomial distribution.
}
\examples{
if (has_opencl()) {
  n <- 5L

  dbinom_raw_opencl(rep(6, n), size = 10, prob = 0.3, fallback = FALSE, verbose = TRUE)
  dbinom_opencl(rep(6, n), size = 10, prob = 0.3, fallback = FALSE, verbose = TRUE)
  pbinom_opencl(q = 6, size = 10, prob = 0.3, fallback = FALSE, verbose = TRUE)
  qbinom_opencl(rep(0.8, n), size = 10, prob = 0.3, fallback = FALSE, verbose = TRUE)
  rbinom_opencl(n, size = 10, prob = 0.3, fallback = FALSE, verbose = TRUE)
} else {
  message("OpenCL unavailable; skipping GPU-only example.")
}
}
