% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chisq_opencl.R
\name{pchisq_opencl}
\alias{pchisq_opencl}
\alias{qchisq_opencl}
\title{The Chi-squared Distribution (OpenCL linkage subset)}
\usage{
pchisq_opencl(n, x, df, ncp, fallback = TRUE, verbose = FALSE)

qchisq_opencl(n, p, df, ncp, fallback = TRUE, verbose = FALSE)
}
\arguments{
\item{n}{Number of observations. Non-negative integer scalar.}

\item{x}{Numeric scalar quantile.}

\item{df}{Degrees of freedom (must be > 0).}

\item{ncp}{Non-centrality parameter (must be >= 0).}

\item{fallback}{Logical; if \code{TRUE}, fall back to CPU behavior on OpenCL error.}

\item{verbose}{Logical; print fallback/error diagnostics.}

\item{p}{Numeric scalar probability in \code{[0, 1]}.}
}
\value{
Numeric vector of length \code{n}.
}
\description{
OpenCL-backed non-central chi-squared distribution and quantile wrappers.
}
\examples{
if (has_opencl()) {
  n <- 5L

  pchisq_opencl(n, x = 4.5, df = 6, ncp = 1.5, fallback = FALSE, verbose = TRUE)
  qchisq_opencl(n, p = 0.8, df = 6, ncp = 1.5, fallback = FALSE, verbose = TRUE)
} else {
  message("OpenCL unavailable; skipping GPU-only example.")
}
}
