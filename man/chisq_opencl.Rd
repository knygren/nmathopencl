% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chisq_opencl.R
\name{dchisq_opencl}
\alias{dchisq_opencl}
\alias{pchisq_opencl}
\alias{qchisq_opencl}
\alias{rchisq_opencl}
\title{The Chi-squared Distribution (OpenCL)}
\usage{
dchisq_opencl(
  x,
  df,
  ncp = 0,
  log = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

pchisq_opencl(
  q,
  df,
  ncp = 0,
  lower.tail = TRUE,
  log.p = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

qchisq_opencl(
  p,
  df,
  ncp = 0,
  lower.tail = TRUE,
  log.p = FALSE,
  opencl_parallel = NA,
  fallback = FALSE,
  verbose = FALSE
)

rchisq_opencl(n, df, ncp = 0, fallback = FALSE, verbose = FALSE)
}
\arguments{
\item{x}{Numeric vector of quantiles for \code{dchisq_opencl}.}

\item{df}{Degrees of freedom (must be > 0).}

\item{ncp}{Non-centrality parameter (must be >= 0).}

\item{log}{\code{log} flag for densities (\code{stats} \emph{d}-family semantics).}

\item{opencl_parallel}{Dispatch hint \code{(TRUE,FALSE,NA)} for \emph{p}/\emph{q}
wrappers on this page; parallel kernels reserved.}

\item{fallback}{When \code{TRUE} while \code{\link{has_opencl}()} reports OpenCL present, recover with CPU if the OpenCL call fails. Ignored when the runtime reports no OpenCL. \code{dchisq_opencl} defaults \code{FALSE}; distribution/quantile and \code{rchisq_opencl} remain \code{TRUE} temporarily (\file{inst/OPENCL_PGAMMA_UTILS_KERNEL_FALLBACK_TEMP.md}); pass explicit \code{fallback} to override.}

\item{verbose}{Logical; print fallback/error diagnostics.}

\item{q}{Numeric vector of quantiles for \code{pchisq_opencl}; recycled like \code{stats::pchisq}.}

\item{lower.tail, log.p}{Tail/log-\emph{p} inputs (\code{stats} meanings).}

\item{p}{Numeric vector of probabilities for \code{qchisq_opencl} (like \code{stats::qchisq}).}

\item{n}{Number of observations (non-negative integer scalar). Used only by \code{rchisq_opencl};
\code{dchisq_opencl} takes vector \code{x} first (like \code{stats::dchisq}).}
}
\value{
Numeric vector of length \code{n}.
}
\description{
OpenCL-backed density, distribution, quantile, and random generation wrappers
for the chi-squared distribution, including non-central paths via \code{ncp}.
}
\examples{
if (has_opencl()) {
  n <- 5L

  dchisq_opencl(rep(4.5, n), df = 6, ncp = 0, fallback = FALSE, verbose = TRUE)
  pchisq_opencl(q = 4.5, df = 6, ncp = 0, fallback = FALSE, verbose = TRUE)
  qchisq_opencl(rep(0.8, n), df = 6, ncp = 0, fallback = FALSE, verbose = TRUE)
  rchisq_opencl(n, df = 6, ncp = 0, fallback = FALSE, verbose = TRUE)
} else {
  message("OpenCL unavailable; skipping GPU-only example.")
}
}
