% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multinomial_opencl.R
\name{rmultinom_opencl}
\alias{rmultinom_opencl}
\title{The Multinomial Distribution (OpenCL linkage subset)}
\usage{
rmultinom_opencl(n, size, prob, fallback = FALSE, verbose = FALSE)
}
\arguments{
\item{n}{Number of observations. Non-negative integer scalar.}

\item{size}{Number of trials per draw (non-negative integer scalar).}

\item{prob}{Probability of the first category in \code{[0, 1]}.}

\item{fallback}{When \code{TRUE} while \code{\link{nmathopencl_has_opencl}()} reports OpenCL present, recover with CPU if the OpenCL call fails. Ignored when the runtime reports no OpenCL (CPU path is chosen automatically). Defaults to \code{FALSE}.}

\item{verbose}{Logical; print fallback/error diagnostics.}
}
\value{
Integer matrix with 2 rows and \code{n} columns.
}
\description{
OpenCL-backed linkage wrapper for multinomial sampling. The current OpenCL
kernel supports a 2-category multinomial parameterization via scalar
\code{prob}, and returns a \code{2 x n} count matrix.
}
\examples{
n <- 5L
if (!nmathopencl_has_opencl() || identical(Sys.getenv("NOT_CRAN"), "true")) {
  rmultinom_opencl(n, size = 12L, prob = 0.4, fallback = FALSE, verbose = TRUE)
} else {
  stats::rmultinom(n, size = 12L, prob = c(0.4, 0.6))
}
}
