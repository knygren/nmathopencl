% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attach_kernel_dependency_tags.R
\name{attach_kernel_dependency_tags}
\alias{attach_kernel_dependency_tags}
\title{Attach Dependency Tags to a Sortable Kernel Library}
\usage{
attach_kernel_dependency_tags(library_dir, dry_run = FALSE)
}
\arguments{
\item{library_dir}{Directory containing \code{.cl} files with \verb{@depends} tags.}

\item{dry_run}{Logical; if \code{TRUE}, compute tags and reports without writing.}
}
\value{
A list with:
\describe{
\item{ok}{Logical success flag.}
\item{message}{Human-readable summary.}
\item{sorted}{Sorted records data frame.}
\item{unresolved}{Unresolved records data frame (empty on success).}
\item{cycles}{Cycle report data frame (empty on success).}
\item{tags}{Per-file tag data frame with \code{source_origin}, \code{source_type},
\code{includes}, \code{depends}, \code{provides}, \code{load_order}, \code{all_depends}, and
\code{all_depends_count} (present on success).}
\item{header_functions}{Functions declared in header files without
\code{attribute_hidden}, including declaring header, inferred definition file,
declaration signature, \code{define_alias}, and \code{all_depends} for the
definition file.}
}
}
\description{
Compute and attach derived tags to each \code{.cl} file in a library:
\verb{@load_order}, \verb{@all_depends}, and \verb{@all_depends_count}.
}
\details{
Tags are written only if dependency sorting fully succeeds. If unresolved
files remain, no files are modified and a cycle report is returned so source
refactoring can be prioritized.
}
