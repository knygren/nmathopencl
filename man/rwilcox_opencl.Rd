% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rwilcox_opencl.R
\name{rwilcox_opencl}
\alias{rwilcox_opencl}
\title{OpenCL-backed Wilcoxon random generation with graceful fallback}
\usage{
rwilcox_opencl(n, m, nn, fallback = TRUE, verbose = FALSE)
}
\arguments{
\item{n}{Number of observations. Non-negative integer scalar.}

\item{m}{Number of observations in one sample (must be > 0).}

\item{nn}{Number of observations in the other sample (must be > 0).}

\item{fallback}{Logical; if \code{TRUE}, fall back to \code{\link[stats]{rwilcox}}
when OpenCL is unavailable or fails.}

\item{verbose}{Logical; print informational fallback messages.}
}
\value{
Numeric vector of length \code{n}.
}
\description{
OpenCL-backed Wilcoxon random generation with graceful fallback
}
\section{Known OpenCL limitations}{

The OpenCL path for \code{rwilcox_opencl()} is a known high-risk linkage
target because translated Wilcoxon code paths can require allocator/runtime
symbols not fully available on device builds. CPU fallback is recommended for
production use until full device-safe allocation shims are in place.
}

\examples{
# OpenCL Wilcoxon paths may require allocator/runtime symbols not always available.
# Keeping this commented avoids flaky example failures across devices.
# rwilcox_opencl(5, m = 5, nn = 7)
}
