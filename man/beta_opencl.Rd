% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_opencl.R
\name{pbeta_opencl}
\alias{pbeta_opencl}
\alias{qbeta_opencl}
\title{The Beta Distribution (OpenCL linkage subset)}
\usage{
pbeta_opencl(n, x, a, b, ncp, fallback = TRUE, verbose = FALSE)

qbeta_opencl(n, p, a, b, ncp, fallback = TRUE, verbose = FALSE)
}
\arguments{
\item{n}{Number of observations. Non-negative integer scalar.}

\item{x}{Numeric scalar quantile in \code{[0, 1]}.}

\item{a}{Shape1 parameter (must be > 0).}

\item{b}{Shape2 parameter (must be > 0).}

\item{ncp}{Non-centrality parameter (must be >= 0).}

\item{fallback}{Logical; if \code{TRUE}, fall back to CPU behavior on OpenCL error.}

\item{verbose}{Logical; print fallback/error diagnostics.}

\item{p}{Numeric scalar probability in \code{[0, 1]}.}
}
\value{
Numeric vector of length \code{n}.
}
\description{
OpenCL-backed distribution and quantile wrappers for beta-family linkage checks.
}
\section{Known OpenCL limitations}{

\code{qbeta_opencl()} (non-central path via \code{qnbeta}) may be slow or hit
device/runtime resource limits for difficult parameter regions on some GPUs.
Use small linkage-smoke inputs when validating kernel wiring.
}

\examples{
if (has_opencl()) {
  n <- 5L

  pbeta_opencl(n, x = 0.6, a = 2.5, b = 4, ncp = 1.1, fallback = FALSE, verbose = TRUE)
  qbeta_opencl(n, p = 0.8, a = 2.5, b = 4, ncp = 1.1, fallback = FALSE, verbose = TRUE)
} else {
  message("OpenCL unavailable; skipping GPU-only example.")
}
}
