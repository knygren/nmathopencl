% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opencltools_reexports.R
\name{load_library_for_kernel}
\alias{load_library_for_kernel}
\title{Load a Minimal OpenCL Library Subset for a Single Kernel}
\usage{
load_library_for_kernel(
  kernel_path,
  library_dir,
  depends_tag = "all_depends",
  index = NULL
)
}
\arguments{
\item{kernel_path}{Path to a single \code{.cl} kernel file.  The file is scanned
for the annotation tag given by \code{depends_tag}.}

\item{library_dir}{Path to the library directory containing the \code{.cl} source
files (e.g. \code{system.file("cl/nmath", package = "opencltools")}).}

\item{depends_tag}{Name of the annotation tag in the kernel file that lists
the required library file stems.  Defaults to \code{"all_depends"}.  For
kernels annotated with \verb{@all_depends_nmath}, pass
\code{depends_tag = "all_depends_nmath"}.}

\item{index}{Optional RDS list; \code{NULL} triggers lazy reads.}
}
\value{
A character vector subclass \verb{nmathopencl_concatenated_lib} holding
concatenated sources (often length 1; blank annotations yield length-zero
concatenation). Attachments describe requested and loaded library stems,
paths, and byte size; see \link[opencltools:kernel_lib_subset_printing]{print methods}.
}
\description{
Given a single kernel \code{.cl} file and a library directory (with an associated
dependency index), reads the annotation tag that lists needed library files
and returns their source code concatenated in the correct dependency order.
}
\details{
For repeated calls in R code, loading \code{kernel_dependency_index.rds} once
and passing \code{index =} avoids redundant disk reads.  The bundled
\verb{cl/nmath} directory ships \code{kernel_dependency_index.rds} beside the
\code{.cl} files; use \link[opencltools]{write_kernel_dependency_index} to regenerate it after
porting (for example via \verb{nmathtools/port_inst_cl_nmath_from_src.R} in the
\code{openclport} package source tree).

\if{html}{\out{<div class="sourceCode r">}}\preformatted{lib_dir <- system.file("cl/nmath", package = "opencltools")
kpath <- system.file("cl/src/dnorm_kernel.cl", package = "opencltools")
src <- load_library_for_kernel(
  kpath, lib_dir,
  depends_tag = "all_depends_nmath")
}\if{html}{\out{</div>}}

Subsets come only from each launcher file's transitive
\code{@all_depends_nmath}-style annotations (use \code{\link[opencltools]{attach_cross_library_tags}}
where dependency graphs span libraries). Deliberately loading every \verb{.cl} under
\verb{cl/nmath} remains available via \code{\link[opencltools]{load_kernel_library}(..., "nmath")}
where appropriate.

When \verb{inst/extdata/opencl_known_failures.json} matches the launcher path or
the declared / loaded stems, \code{\link{warning}(...)} points to fragile ports.
}
\examples{
############################ Start of load_library_for_kernel example ########################

## Small library (fast; runs on CRAN check)
lib_small <- system.file("cl/nmath_small", package = "opencltools")
kpath     <- system.file("cl/src/dnorm_kernel.cl", package = "opencltools")
idx_small <- write_kernel_dependency_index(library_dir = lib_small, write = FALSE)
src_small <- load_library_for_kernel(
  kpath, lib_small,
  depends_tag = "all_depends_nmath",
  index       = idx_small
)
nzchar(src_small)

\donttest{
## Full nmath (slow; rebuilds index over all shards)
lib_dir <- system.file("cl/nmath", package = "opencltools")
idx   <- write_kernel_dependency_index(library_dir = lib_dir, write = FALSE)
src <- load_library_for_kernel(
  kpath, lib_dir,
  depends_tag = "all_depends_nmath",
  index       = idx
)
print(src)
nzchar(src)
}

###############################################################################
## End of load_library_for_kernel example
###############################################################################
}
\seealso{
\link[opencltools]{extract_library_subset}

\link[opencltools:kernel_lib_subset_printing]{printing methods}

\link[opencltools]{write_kernel_dependency_index}

Other OpenCL kernel library subsets: 
\code{\link[opencltools]{extract_library_subset}()},
\code{\link[opencltools]{write_kernel_dependency_index}()}
}
