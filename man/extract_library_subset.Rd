% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cl_library_utils.R
\name{extract_library_subset}
\alias{extract_library_subset}
\title{Extract a Minimal Library Subset for a Set of Kernels}
\usage{
extract_library_subset(
  kernel_paths,
  library_dir,
  dest_dir,
  depends_tag = "all_depends",
  index = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{kernel_paths}{Character vector of paths to kernel \code{.cl} files.  Each
file is scanned for the annotation tag given by \code{depends_tag}.}

\item{library_dir}{Path to the source library directory.}

\item{dest_dir}{Path to the destination directory.  Must already exist; a
warning is issued and the function returns invisibly if it does not.
In addition to the \code{.cl} files, \code{kernel_dependency_index.rds} is copied
here so the extracted subset can be used immediately with a pre-loaded
index.}

\item{depends_tag}{Name of the annotation tag listing library file stems.
Defaults to \code{"all_depends"}.  Pass \code{"all_depends_nmath"} for kernels that
annotate their nmath dependencies with that tag.}

\item{index}{Pre-loaded dependency index.  If \code{NULL}, read from
\code{file.path(library_dir, "kernel_dependency_index.rds")} with a
\code{message()} nudging toward the recommended pattern.}

\item{overwrite}{Logical; if \code{FALSE} (default) existing files in \code{dest_dir}
are not overwritten — the copy is skipped and \code{copied = FALSE} in the
returned data frame.}
}
\value{
A data frame (returned invisibly) with one row per copied file
(\code{.cl} files in dependency order, followed by
\code{kernel_dependency_index.rds}) and columns:
\describe{
\item{\code{stem}}{Stem name (filename without \code{.cl}), or
\code{"kernel_dependency_index.rds"} for the index.}
\item{\code{source}}{Full path to the source file in \code{library_dir}.}
\item{\code{dest}}{Full path to the destination file in \code{dest_dir}.}
\item{\code{copied}}{\code{TRUE} if the file was copied; \code{FALSE} if it already
existed and \code{overwrite = FALSE}.}
}
}
\description{
Given one or more kernel \code{.cl} files and a library directory, determines the
minimal set of library files required (union of all kernels' dependency
annotations) and copies them — in dependency order — to a destination
directory.
}
\details{
Use this to populate a project-local copy containing only the library files
actually needed by your kernels.  The result can then be committed alongside
your kernel files, removing a runtime dependency on the full library.

It is strongly recommended to supply a pre-loaded \code{index}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{idx <- readRDS(
  system.file(
    "cl/nmath/kernel_dependency_index.rds",
    package = "nmathopencl"))
result <- extract_library_subset(
  kernel_paths, nmath_dir, dest_dir, index = idx)
}\if{html}{\out{</div>}}
}
\seealso{
\link{load_library_for_kernel}

\link{write_kernel_dependency_index}
}
