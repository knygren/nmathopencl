% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opencltools_reexports.R
\name{attach_kernel_call_tags}
\alias{attach_kernel_call_tags}
\title{Attach Library Call Tags to Kernel Files}
\usage{
attach_kernel_call_tags(
  kernel_paths,
  library_dir,
  library_tag,
  overwrite_existing = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{kernel_paths}{Character vector of paths to kernel \code{.cl} files.}

\item{library_dir}{Path to the pre-annotated library directory.  Each
\code{.cl} file in this directory must carry a \verb{@provides} annotation listing
the symbols it exports.}

\item{library_tag}{String tag suffix used for annotation names, e.g.
\code{"nmath"}.  Must not contain spaces or regex special characters.}

\item{overwrite_existing}{Logical; if \code{FALSE} (default), skip files that
already carry a \verb{@calls_<library_tag>} annotation.  Set to \code{TRUE} to
re-scan and overwrite (also clears any existing \verb{@all_depends_<tag>} so
\link[opencltools]{attach_cross_library_tags} re-computes it cleanly).}

\item{dry_run}{Logical; if \code{TRUE}, compute tags but do not write any files.}
}
\value{
A data frame (returned invisibly) with one row per kernel file and
columns:
\describe{
\item{\code{file}}{Basename of the kernel file.}
\item{\code{calls}}{Comma-separated library symbols detected in source.}
\item{\code{depends}}{Comma-separated shard stems for the detected symbols.}
\item{\code{opencl_builtins}}{Comma-separated OpenCL builtins detected, or
empty string if none.}
\item{\code{changed}}{\code{TRUE} if the file was (or would be) modified.
\code{NA} means the file was skipped (already tagged).}
}
}
\description{
Scans kernel \code{.cl} source files for calls to functions provided by a
pre-annotated library, then writes the discovered dependencies as annotation
tags directly into the kernel files.
}
\details{
For a library such as \pkg{nmathopencl}, each \code{.cl} shard carries a \verb{@provides}
annotation listing the symbols it defines.  This function builds a
\strong{provides map} from those annotations (symbol \eqn{\to} shard stem), scans
each kernel's source (with comments and string literals stripped) for
matching function calls, and writes four annotation tags at the top of each
kernel file:

\describe{
\item{\verb{@library_deps: <library_tag>}}{Library name (written if absent).}
\item{\verb{@calls_<library_tag>: sym1, sym2}}{Symbols from the library actually
called in this kernel.}
\item{\verb{@depends_<library_tag>: stem1, stem2}}{Library shard stems that
define the called symbols.}
\item{\verb{@calls_opencl_builtin: sym | (none)}}{Detected OpenCL work-item and
synchronization \code{builtins} (standard math \code{builtins} excluded).}
}

\strong{Two-step tagging workflow:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Step 1 — this function: infer direct library calls from source
nmath_dir <- system.file("cl/nmath", package = "nmathopencl")
attach_kernel_call_tags(
  kernel_paths = list.files("inst/cl/src", "\\\\.cl$", full.names = TRUE),
  library_dir  = nmath_dir,
  library_tag  = "nmath"
)

# Step 2 — expand to full transitive closure
attach_cross_library_tags(
  kernel_paths = list.files("inst/cl/src", "\\\\.cl$", full.names = TRUE),
  library_dir  = nmath_dir,
  depends_tag  = "depends_nmath"
)
}\if{html}{\out{</div>}}
}
\examples{
############################ Start of kernel_tagging_workflow example ########################

lib_dir <- system.file("cl/ex_glmbayes_nmath", package = "opencltools")
kernels <- list.files(
  system.file("cl/ex_glmbayes_draft_src", package = "opencltools"),
  pattern = "\\\.cl$", full.names = TRUE
)

# Step 1: scan draft kernels for library calls (read-only dry run)
step1 <- attach_kernel_call_tags(
  kernel_paths = kernels,
  library_dir  = lib_dir,
  library_tag  = "nmath",
  dry_run      = TRUE
)
step1

# Step 2: expand transitive closure (small nmath library; runs on CRAN check)
nmath_small <- system.file("cl/nmath_small", package = "opencltools")
tagged      <- system.file("cl/src/dnorm_kernel.cl", package = "opencltools")
idx_small   <- write_kernel_dependency_index(library_dir = nmath_small, write = FALSE)

step2_small <- attach_cross_library_tags(
  kernel_paths = tagged,
  library_dir  = nmath_small,
  depends_tag  = "depends_nmath",
  index        = idx_small,
  dry_run      = TRUE
)
nrow(step2_small)

\donttest{
# Step 2: full nmath (slow)
nmath_dir <- system.file("cl/nmath", package = "opencltools")
idx       <- write_kernel_dependency_index(library_dir = nmath_dir, write = FALSE)

step2 <- attach_cross_library_tags(
  kernel_paths = tagged,
  library_dir  = nmath_dir,
  depends_tag  = "depends_nmath",
  index        = idx,
  dry_run      = TRUE
)
step2
}

###############################################################################
## End of kernel_tagging_workflow example
###############################################################################
}
\seealso{
\link[opencltools]{attach_cross_library_tags}, \link[opencltools]{attach_kernel_dependency_tags}
}
